export const VIEWPORT_HEIGHT = window.innerHeight * 0.95;
export const VIEWPORT_WIDTH = window.innerWidth * 0.95;
export const MENOR_LARGURA = VIEWPORT_HEIGHT < VIEWPORT_WIDTH ? VIEWPORT_HEIGHT : VIEWPORT_WIDTH;
